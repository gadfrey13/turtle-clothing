import data from '../../component/directory/directory.data';

const directoryReducer = (state = data, action) => {
    switch(action.type){
        default:
            return state
    }
}

export default directoryReducer;